defaults:
  - _self_

paths:
  result_p: "release_ensemble_tensor_data/test_corrected/"
  output_dir: "release_ensemble_summarized_results/"
  model_p: "release_ensemble_performance/"

experiment:
  expected_samples: 100
  verbose: 0

restrict_to: 3
modus: mean_predictions


model: SimpleLinear
size: small

performance_score: "SHD individual"

normalize_predictions: True 

